<Page x:Class="QRD.UI.Views.ScannerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource BgBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Project Scanner" FontSize="24" FontWeight="Bold"
                       Foreground="{StaticResource TextBrush}"/>
            <TextBlock Text="Select a folder to scan and explore its file tree."
                       Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Scan controls -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Project Path" FontWeight="SemiBold"
                           Foreground="{StaticResource TextBrush}" Margin="0,0,0,6"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="PathBox" Grid.Column="0"
                             Padding="10,8" FontSize="13"
                             BorderBrush="{StaticResource BorderBrush}"
                             Margin="0,0,8,0"/>
                    <Button Grid.Column="1" Content="Browse…"
                            Style="{StaticResource SecondaryButton}"
                            Click="Browse_Click" Margin="0,0,8,0"/>
                    <Button Grid.Column="2" Content="Scan"
                            Style="{StaticResource PrimaryButton}"
                            Click="Scan_Click" x:Name="ScanButton"/>
                </Grid>

                <!-- Progress -->
                <Grid x:Name="ProgressPanel" Visibility="Collapsed" Margin="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <ProgressBar x:Name="ScanProgress" Grid.Row="0"
                                 Height="6" Minimum="0" Maximum="100"
                                 Foreground="{StaticResource AccentBrush}"
                                 Background="{StaticResource BorderBrush}"/>
                    <TextBlock x:Name="ProgressText" Grid.Row="1" Margin="0,4,0,0"
                               Foreground="{StaticResource MutedBrush}" FontSize="12"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Results: file tree + stats -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- File tree -->
            <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                    CornerRadius="8" Margin="0,0,12,0">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="File Tree"
                               FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"
                               Padding="12,12,12,8"/>
                    <TreeView x:Name="FileTree" BorderThickness="0"
                              Background="Transparent"
                              SelectedItemChanged="FileTree_SelectedItemChanged"/>
                </DockPanel>
            </Border>

            <!-- File detail / stats panel -->
            <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                    CornerRadius="8">
                <DockPanel>
                    <TextBlock x:Name="DetailHeader" DockPanel.Dock="Top"
                               Text="Select a file to preview"
                               FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"
                               Padding="12,12,12,8"/>
                    <ScrollViewer>
                        <StackPanel x:Name="DetailPanel" Margin="12,0,12,12"/>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
